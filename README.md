# Instant Mail

Free disposable temporary email. Built with React, Vite, and the [Mail.tm](https://mail.tm/) API.

## Scripts

- `npm run dev` – local development
- `npm run build` – production build
- `npm run preview` – preview production build
